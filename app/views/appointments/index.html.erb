<div class="page-header">
  <%= link_to new_appointment_path, class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    New Appointment
  <% end %>
  <% if @current_user.admin? %>
    <h1><%= @appointments.count %> Appointments</h1>
  <% else %>
    <h1>My Appointments</h1>
  <% end %>
</div>

<div class="table-responsive appointments">
  <table class="table table-hover">
    <thead>
      <tr>
            <% if @current_user.admin? %>
              <th>Name<br><%= select(:user, :last_name, User.uniq.order(last_name: :asc).pluck(:last_name)) %></th>
              <th>Email<br><%= select(:user, :email, User.uniq.pluck(:email)) %></th>
              <th>Date<br><%= select(:appointment, :date, Appointment.uniq.order(date: :asc).pluck(:date)) %></th>
              <th>Time<br><%= select(:appointment, :time, Appointment.uniq.order(time: :asc).pluck(:time)) %></th>
              <th>Comment<br><%= select(:appointment, :comment, Appointment.uniq.pluck(:comment)) %></th>
            <% else %>
              <th>Date</th>
              <th>Time</th>
              <th>Comment</th>
            <% end %>
            <th>View</th>
            <th>Edit</th>
            <th>Delete</th>
      </tr>
    </thead>

    <tbody>
      <%= content_tag_for(:tr, @appointments) do |appointment| %>
            <% if @current_user.admin? %>
              <% @user = User.find(appointment.user_id) %>
              <th><%= @user.first_name %> <%= @user.last_name %></th>
              <th><%= User.find(appointment.user_id) %></th>
            <% end %>
            <td><%= appointment.date %></td>
            <td><%= appointment.time.strftime("%I:%M %p") %></td>
            <td><%= appointment.comment %></td>
            <td><%= link_to 'Show', appointment %></td>
        <td><%= link_to 'Edit', edit_appointment_path(appointment) %></td>
        <td><%= link_to 'Delete', appointment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      <% end %>
    </tbody>
  </table>
</div>
